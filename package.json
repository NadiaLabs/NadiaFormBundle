{
    "name": "nadia-form-bundle",
    "version": "0.1.0",
    "dependencies": {
        "jquery": "^3.3"
    },
    "devDependencies": {
        "@symfony/webpack-encore": "^0.28",
        "concurrently": "^4.1",
        "core-js": "^3.0",
        "node-sass": "^4.12",
        "regenerator-runtime": "^0.13",
        "sass-loader": "^7.1",
        "webpack-notifier": "^1.6"
    },
    "license": "MIT",
    "scripts": {
        "dev-server": "encore dev-server",
        "dev-server-https": "encore dev-server --https",
        "dev": "encore dev",
        "watch": "encore dev --watch",
        "build": "encore production --progress",
        "servers": "concurrently \"php bin/console server:run\" \"npm run dev-server\""
    }
}
